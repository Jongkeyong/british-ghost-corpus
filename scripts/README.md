# Scripts

This directory is reserved for code used to audit, validate, summarize, or transform corpus metadata and derived results.

Scripts deposited here should record:

- expected inputs and outputs;
- software and package versions;
- parameters and defaults;
- known limitations;
- whether the script changes source files or writes new derivatives; and
- the corpus version against which it was run.

No script should silently overwrite the preserved source collection.
